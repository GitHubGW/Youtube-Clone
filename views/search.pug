extends layouts/main.pug
include mixins/videoBlock.pug

block content
  .search_header 
    h3 You are searching #{searchingBy}
  .search_videos
    each item in videos
      +videoBlock({
        title: item.title,
        views: item.views,
        videoFile: item.videoFile,
        })
